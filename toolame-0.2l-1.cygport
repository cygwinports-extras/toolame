DESCRIPTION="MPEG-1 layer 2 audio encoder"
HOMEPAGE="http://toolame.sourceforge.net/"
SRC_URI="mirror://sourceforge/${PN}/${PN}-${PV//.}.tgz"
SRC_DIR="${PN}-${PV//.}"

src_compile() {
	lndirs
	cd ${B}
	cygmake
}

src_install() {
	cd ${B}
	dobin toolame.exe
}

DOCS="FUTURE LGPL.txt"
HTMLDOCS="html/*.html"
